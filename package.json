{
  "name": "artwalkapp_heroku_mongodb",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd server && node server.js",
    "dev": "cd client && npm start",
    "test": "cd client && npm test",
    "preinstall": "cd server && npm install",
    "install": "cd client && npm install",
    "build": "cd client && npm run build"
  },
  "engines": {
    "node": "14.17.5",
    "npm": "8.1.0"
  }
}
