{
  "name": "artwalkapp_heroku_mongodb",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd clon_auth_backend && node server.js",
    "dev": "cd clon_auth_frontend && npm start",
    "preinstall": "cd clon_auth_backend && npm install",
    "install": "cd clon_auth_frontend && npm install",
    "build": "cd clon_auth_frontend && npm run build"

    
  },
  "eslintConfig": {
    "extends": "react-app"
  }
}
